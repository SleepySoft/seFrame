你作为一个专业的python程序员，基于python3.8编写程序。你将会用到pyqt5、pandas、openyxl完成下面我所描述的功能，每个功能需要使用英文进行python标准格式的注释。理解了就说“好的”。



根据以下定义，帮我补全这个dict：

#define DF_TYPE_BYTE            ((DF_TYPE_T)0)  /**< BYTE: Unsigned 8-bit integer, typically used for buffers */
#define DF_TYPE_CHAR            ((DF_TYPE_T)1)  /**< CHAR: Signed 8-bit integer, typically used for strings */
#define DF_TYPE_INT16           ((DF_TYPE_T)2)  /**< INT16: Signed 16-bit integer */
#define DF_TYPE_UINT16          ((DF_TYPE_T)3)  /**< UINT16: Unsigned 16-bit integer */
#define DF_TYPE_INT32           ((DF_TYPE_T)4)  /**< INT32: Signed 32-bit integer */
#define DF_TYPE_UINT32          ((DF_TYPE_T)5)  /**< UINT32: Unsigned 32-bit integer */
#define DF_TYPE_FLOAT32         ((DF_TYPE_T)6)  /**< FLOAT32: 32-bit floating point number */
#define DF_TYPE_FLOAT64         ((DF_TYPE_T)10) /**< FLOAT32: 64-bit floating point number */

TYPE_MAPPING = {
    'DF_TYPE_BYTE': 'uint8_t',
}



写一个类：DataFrameParser，用来分析指定excel文件，并按一定的规则对其进行转化。

首先使用openyxl打开指定的excel文件，然后调用下面的各个函数对其进行处理：

实现一个成员函数parse_enum，分析excel第3页。其第一行是表头，从第二行开始，第1列是枚举类型名，第2列是枚举值，第3列是枚举项名，第4列是该项对应文本。
从第一行遍历到最后一行，如果第1列不为空，则为一个新的枚举开始，随后的行只要第2列和第3列有内容的，都归于该枚举之下，直到下一个新的枚举开始。
最后组织的形式为：{枚举名: {枚举值1:(枚举项1, 对应文本), 枚举值2:(枚举项2, 对应文本)}}
保存到self.enum_table中。

实现一个成员函数parse_bit_field，分析excel第4页。其第一行是表头，从第二行开始，第1列是位域名，第2列是位域对应的位编号，第3列是该位的名称，第4列是该项对应文本。分析方法及数据组织方法基本和上面的枚举相同。
保存到self.bit_field_table中。




把以下excel表头转换为字符串常量定义：
Path	Name	Description	Type	Length	Style	Range	Default	Unit	Blob	Feature





实现一个成员函数parse_data_frame(self, workbook)，载入excel第二页到dataframe中，然后依次调用以下函数：

pre_process(df)：将






定义一个函数 duplicate_rows，它接受四个参数：原始的 DataFrame，要复制的行索引列表 rows_to_copy，要保留的列名列表 columns_to_keep，以及复制的次数 N。
在函数内部，首先完整地复制指定行的所有数据。
然后，对于复制的行，保留 columns_to_keep 中指定的列，并清空其他列。
将这些复制的行重复 N 次，形成一个新的 DataFrame。
确定插入点，即要复制的行的下一行位置。
使用 pd.concat 将原始 DataFrame 分为两部分：插入点之前的所有行和插入点之后的所有行。
将复制的行 DataFrame 插入到这两部分之间。
返回合并后的新 DataFrame。








完成这个函数：

```
    def expand_data_frame_rows(df: pd.DataFrame, data_path: str, same_path_rows: list):
        expanded_path = LevelingPathParser.expand_array_path(data_path)
        duplicate_row_indexes = [index for index, _ in same_path_rows]
        expanded_df = duplicate_rows(df, duplicate_row_indexes, [], len(expanded_path))
```

其中same_path_rows为[(index, row)]，调用duplicate_rows()后，same_path_rows包含的行被复制len(expanded_path)份，每份称为一组，并插入到原始的row后面。
我需要为每组的'expand_path'列赋值，每组依次赋值为expanded_path的一项。


